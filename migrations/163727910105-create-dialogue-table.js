exports.up = async function up(sql) {
  console.log('Creating dialogue table...');
  await sql`
		CREATE TABLE dialogue (
			id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
			villager_id integer UNIQUE NOT NULL,
			question_text TEXT,
			answer1_text TEXT,
			answer2_text TEXT,
			answer3_text TEXT,
			fail_text TEXT,
			succeed_text TEXT
		);
	`;
};

exports.down = async function down(sql) {
  console.log('Dropping dialogue table...');
  await sql`DROP TABLE dialogue`;
};
