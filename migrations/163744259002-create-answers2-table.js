exports.up = async function up(sql) {
  console.log('Creating answers2 table...');
  await sql`
		CREATE TABLE answers2 (
			id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
			answer_id integer,
			answer_text TEXT,
			correct boolean
		);
	`;
};

exports.down = async function down(sql) {
  console.log('Dropping answers2 table...');
  await sql`DROP TABLE answers2`;
};
